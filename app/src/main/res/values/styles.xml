<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#10BD10</item>
        <item name="colorPrimaryDark">@color/darkgreen</item>
        <item name="colorAccent">@color/gold</item>
    </style>

    <style name="BaseThemes" parent="Theme.AppCompat.Light.NoActionBar">
<!--        <item name="android:statusBarColor">#39383e</item>-->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="BaseThemes1" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">#10BD10</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="ThemeSpinner">
        <!-- Color when pressed -->
        <item name="colorAccent">#e6e6e6</item>
        <!-- Default color for the dropdown arrow and line -->
        <item name="colorControlNormal">#e6e6e6</item>
    </style>

    <style name="data_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

</resources>
